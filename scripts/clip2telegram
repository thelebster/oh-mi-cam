#!/bin/sh
# Wrapper for /clip command - provides immediate feedback
. /etc/webui/motion.conf
duration=${motion_video_duration:-10}
echo "Recording ${duration}s clip..."
/sbin/send2telegram-video -t $duration >> /tmp/clip.log 2>&1 &
